///** @type {import('next').NextConfig} 
//** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['localhost'], // Add your domain here
    },
  }
  
  module.exports = nextConfig